# 关卡上传功能部署指南

## 🎯 功能说明

现在编辑器支持直接将关卡上传到服务器，无需手动下载和上传文件！

## 💰 费用说明

**完全免费！** 所有推荐方案都有免费额度，足够个人项目使用。

## 🚀 快速开始（推荐：Vercel）

### 步骤 1: 准备代码

项目已经包含了 API 文件：
- `api/levels.js` - API 端点
- `vercel.json` - Vercel 配置

### 步骤 2: 部署到 Vercel

1. **注册账号**
   - 访问 https://vercel.com
   - 使用 GitHub 账号登录（完全免费）

2. **部署项目**
   - 点击 "Add New Project"
   - 导入你的 GitHub 仓库
   - Vercel 会自动检测并部署
   - 等待部署完成（约 1-2 分钟）

3. **获取 API 地址**
   - 部署完成后，你会得到一个地址，例如：`https://my-matchgame.vercel.app`
   - API 端点就是：`https://my-matchgame.vercel.app/api/levels`

### 步骤 3: 配置编辑器

1. 打开游戏，进入关卡编辑器
2. 点击 "配置 API" 按钮
3. 输入你的 API 端点地址（例如：`https://my-matchgame.vercel.app/api/levels`）
4. 点击确定

### 步骤 4: 使用上传功能

1. 在编辑器中编辑关卡
2. 点击 "保存到内存"
3. 点击 "上传到服务器"
4. 等待上传完成 ✅

## 🔧 其他部署方案

### 方案 B: Supabase（推荐用于生产环境）

**优点：**
- 免费额度大（500MB 数据库）
- 自动生成 REST API
- 支持实时同步
- 内置认证系统

**步骤：**

1. 注册 Supabase：https://supabase.com
2. 创建新项目
3. 在 SQL Editor 中执行：

```sql
-- 创建关卡表
CREATE TABLE levels (
  id SERIAL PRIMARY KEY,
  level_data JSONB NOT NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 启用 Row Level Security（可选，用于权限控制）
ALTER TABLE levels ENABLE ROW LEVEL SECURITY;

-- 允许所有人读取和写入（开发阶段，生产环境应该限制）
CREATE POLICY "Allow all" ON levels FOR ALL USING (true);
```

4. 在 Settings > API 中获取：
   - Project URL
   - anon/public key

5. 修改 `api/levels.js` 使用 Supabase API（需要安装 `@supabase/supabase-js`）

### 方案 C: Netlify Functions

类似 Vercel，但使用 Netlify：

1. 注册 Netlify：https://netlify.com
2. 将 `api/levels.js` 移动到 `netlify/functions/levels.js`
3. 导入 GitHub 仓库
4. 自动部署

## 📝 API 使用说明

### API 端点

- **GET** `/api/levels` - 获取所有关卡
- **POST** `/api/levels` - 保存关卡（需要 JSON body: `{ levels: [...] }`）

### 请求示例

```javascript
// 上传关卡
fetch('https://your-api.vercel.app/api/levels', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    levels: [
      { id: 1, name: "关卡1", ... },
      { id: 2, name: "关卡2", ... }
    ]
  })
})
```

### 响应格式

```json
{
  "success": true,
  "message": "关卡已保存",
  "count": 12
}
```

## ⚠️ 注意事项

1. **当前 API 实现是示例代码**
   - 使用内存存储，重启后会丢失
   - 生产环境需要连接数据库

2. **推荐使用 Supabase**
   - 免费且功能完整
   - 自动处理数据持久化
   - 支持实时同步

3. **安全性**
   - 当前实现没有认证
   - 生产环境应该添加用户认证
   - 可以使用 Supabase Auth 或 Vercel 的认证功能

## 🎉 完成！

现在你可以：
1. ✅ 在编辑器中编辑关卡
2. ✅ 一键上传到服务器
3. ✅ 其他玩家可以立即看到新关卡

无需再手动下载和上传文件了！

